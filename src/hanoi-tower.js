const CustomError = require("../extensions/custom-error");

module.exports = function calculateHanoi(disksNumber, turnsSpeed) {
  // let count = Math.pow(2, disksNumber)-1;
  // let sec = count * 1;
  // return{turns: count, seconds: Math.floor(sec)}
};
